terraform {
  required_providers {
    kind = {
      source  = "tehcyx/kind"
      version = "0.0.12" # Verifique a versão mais recente compatível
    }
  }
}

# Provider Kind não requer configuração explícita aqui
